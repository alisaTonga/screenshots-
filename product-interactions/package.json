{
  "name": "metzler-interaction-tests",
  "version": "1.0.0",
  "description": "Interaction/E2E tests for edelstahl-tuerklingel.de product pages (color select, configurator, tabs, add-to-cart)",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
